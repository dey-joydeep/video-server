# REQUIRED: where your videos live (recurse all subfolders)
VIDEO_ROOT=D:\Videos\P

# Server bind + port
BIND=0.0.0.0
PORT=8200

# Where to store the JSON index and thumbnails (relative to project)
DATA_DIR=./data
THUMBS_DIR=./thumbs

# Thumbnail settings
THUMB_WIDTH=320
THUMB_AT_SECONDS=3

# Run a sync on server start? (1=yes, 0=no)
AUTO_SYNC_ON_START=1

# Optional explicit ffmpeg/ffprobe paths (leave empty to use PATH)
# FFMPEG_PATH=C:\ffmpeg\bin\ffmpeg.exe
# FFPROBE_PATH=C:\ffmpeg\bin\ffprobe.exe

# HLS / session settings
HLS_DIR=.hls
HLS_SEG_SEC=4
TOKEN_TTL_SEC=900
TOKEN_PIN_IP=true